# TODO - Render compatibility fixes

- [ ] Step 1: Add Render-compatible start command (create `Procfile`)
- [ ] Step 2: Make settings env vars safer (avoid crash when optional secrets missing)
- [ ] Step 3: Make Gemini init lazy (remove network calls at import time)
- [ ] Step 4: Start scheduler from FastAPI lifespan after Mongo connects
- [ ] Step 5: Update WhatsApp Node defaults to avoid localhost on Render
- [ ] Step 6: Quick runtime sanity checks

